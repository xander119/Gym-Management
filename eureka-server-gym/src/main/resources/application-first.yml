server:
  port: 8002
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: first
  client:
    serviceUrl:
      defaultZone: http://second:8003/eureka/ #注册到另一个Eureka注册中心
    fetch-registry: true
    register-with-eureka: true
